/**
 * Joins class names and resolves Tailwind conflicts (the later class wins).
 * Re-exported so components import it from one place in the design system
 * instead of depending on the `cn` package directly.
 */
export { cn } from "cn";
