import { View } from "react-native";

// Placeholder route; the start screen is built test-first in a later commit.
export default function StartScreen() {
  return <View className="flex-1 bg-background" />;
}
