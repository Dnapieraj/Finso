/*
 * Placeholders until publishing (stage 8 checklist in docs/PRODUCT.md):
 * the real domain, mailbox and data controller replace these values here.
 */

/** Production origin, used for canonical URLs, the sitemap and robots.txt. */
export const siteUrl = "https://finso.app";

/** Contact address for privacy requests and account deletion. */
export const contactEmail = "kontakt@finso.app";

/** Data controller and service provider named in the legal documents. */
export const dataController = "[Administrator — do uzupełnienia]";
